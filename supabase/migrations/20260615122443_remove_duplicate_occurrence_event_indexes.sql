-- These indexes were created manually before the canonical migration and have
-- the same definitions as the canonical indexes retained below.
drop index if exists public.uq_occurrence_event_device_local;
drop index if exists public.idx_occurrence_events_operation;
drop index if exists public.idx_occurrence_events_occurrence;
