import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  closeIconContainer: {
    alignItems: 'flex-end',
    width: '100%',
    marginTop: 10,
    marginRight: 24,
  },
  actionsContainer: {
    flexDirection: 'row',
    justifyContent: 'center',
    gap: 44,
    marginVertical: 12,
  },
});
